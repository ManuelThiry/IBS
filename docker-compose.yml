version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"  # HTTP
      - "443:443"  # HTTPS
    volumes:
      - app-data:/app/wwwroot
    environment:
      - ASPNETCORE_ENVIRONMENT=Production

volumes:
  app-data:
